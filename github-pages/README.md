# GitHub Form

Displays the personnel form at the GitHub Pages address.

`Code.gs` processes submissions and saves responses to Google Sheets.

`Index.html` displays the form, validates entries, and shows submission confirmation.

`appsscript.json` configures the Google Apps Script project.
