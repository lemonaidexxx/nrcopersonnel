{
  "name": "nrcopersonnel",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "node scripts/build.mjs && node scripts/build-google.mjs",
    "test": "node --test tests/*.test.mjs",
    "preview:legacy": "node scripts/preview.mjs"
  }
}
