{
  "name": "nrcopersonnel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "preview": "node scripts/preview.mjs",
    "deploy": "npm run build && npx --yes wrangler@4.94.0 deploy --keep-vars"
  }
}
